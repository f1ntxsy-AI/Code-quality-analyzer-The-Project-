# 🌐 Веб-интерфейс Code Quality Analyzer

Руководство по использованию веб-приложения для анализа качества кода.

---

## 🚀 Запуск

```bash
streamlit run src/web_app.py
```

Приложение откроется в браузере: **http://localhost:8501**

---

## 📋 Режимы работы

### 1. 📝 Вставить код

**Назначение:** Быстрый анализ кода без сохранения файла

**Как использовать:**
1. Выберите режим "📝 Вставить код" в боковой панели
2. Вставьте Python код в текстовое поле
3. Нажмите кнопку "🔍 Анализировать"

**Результат:**
- Общий балл (0-100) и оценка (A-F)
- Разбивка по метрикам с progress bars
- Статистика кода (строки, функции, классы)
- Детальный анализ (сложность, дубликаты, покрытие)
- Рекомендации по улучшению
- Кнопки экспорта отчётов (TXT, JSON, MD)

---

### 2. 📁 Загрузить файл

**Назначение:** Анализ существующего .py файла

**Как использовать:**
1. Выберите режим "📁 Загрузить файл"
2. Нажмите "Browse files" и выберите .py файл
3. Просмотрите код (отображается с подсветкой синтаксиса)
4. Нажмите "🔍 Анализировать"

**Результат:**
- Те же результаты, что и в режиме "Вставить код"
- Имя файла сохраняется в отчёте

---

### 3. 📊 Сравнить файлы

**Назначение:** Сравнение качества двух файлов

**Как использовать:**
1. Выберите режим "📊 Сравнить файлы"
2. Загрузите первый файл (Файл 1)
3. Загрузите второй файл (Файл 2)
4. Нажмите "🔍 Сравнить"

**Результат:**
- Объявление победителя (файл с высшим баллом)
- Таблица сравнения всех метрик
- Progress bars для обоих файлов
- Визуализация различий

**Пример:**
```
🏆 Результаты сравнения

good_code.py          VS          bad_code.py
   100/100 (A)                      65/100 (D)
  🏆 Победитель!
```

---

## 📊 Интерфейс

### Боковая панель (Sidebar)

**⚙️ Настройки**
- Выбор режима работы
- Переключение между режимами

### Основная область

**Режим "Вставить код":**
- Текстовое поле для кода (300px высота)
- Кнопка "🔍 Анализировать"
- Результаты анализа

**Режим "Загрузить файл":**
- File uploader (принимает .py файлы)
- Предпросмотр кода
- Кнопка "🔍 Анализировать"
- Результаты анализа

**Режим "Сравнить файлы":**
- 2 колонки для загрузки файлов
- Кнопка "🔍 Сравнить"
- Таблица сравнения
- Детали для каждого файла

---

## 📈 Отображение результатов

### Общий балл

```
🎯 Общий балл: 100/100

       A         Качество: Отлично
   (зелёный)
```

**Цвета оценок:**
- A: зелёный (#28a745)
- B: светло-зелёный (#5cb85c)
- C: жёлтый (#ffc107)
- D: оранжевый (#fd7e14)
- F: красный (#dc3545)

---

### Метрики

Каждая метрика отображается с:
- Названием и эмодзи
- Progress bar (заполнение по проценту)
- Баллом (текущий/максимальный)

**Пример:**
```
📚 Documentation
████████████░░░░░░░░ 12/15
```

---

### Статистика кода

4 колонки с метриками:
- Строк кода
- Функций
- Классов
- Импортов

---

### Детали анализа

**Развернуть секцию "🔍 Детали анализа":**

1. **Цикломатическая сложность:**
   - Средняя сложность
   - Максимальная сложность
   - Список сложных функций (если есть)

2. **Дублирование кода:**
   - Количество найденных пар
   - Список дубликатов с процентом похожести

3. **Покрытие тестами:**
   - Найдено тестов
   - Процент покрытия

---

### Рекомендации

**Автоматически генерируются на основе:**
- Низкое покрытие docstrings → "Добавьте docstrings"
- Высокая сложность → "Упростите функции"
- Найдены дубликаты → "Устраните дублирование"
- Нет тестов → "Добавьте unit-тесты"
- Проблемы стиля → "Исправьте проблемы стиля"

**Пример:**
```
💡 Рекомендации

✅ Отличное качество кода! Нет замечаний.

или

• Добавьте docstrings (текущее покрытие: 50%)
• Упростите сложные функции (найдено 2)
```

---

### Экспорт отчётов

3 кнопки для скачивания:

**📄 Скачать TXT**
- Текстовый отчёт с ASCII progress bars
- Подходит для консоли

**📊 Скачать JSON**
- Структурированные данные
- Подходит для API/интеграций

**📝 Скачать MD**
- Markdown формат
- Подходит для документации

---

## 🎨 Примеры использования

### Пример 1: Быстрая проверка кода

1. Выберите "📝 Вставить код"
2. Вставьте этот код:

```python
def add(a, b):
    return a + b
```

3. Нажмите "🔍 Анализировать"
4. Получите оценку ~70/100 (C) - нет docstrings и тестов

---

### Пример 2: Анализ с тестами

```python
def add(a, b):
    """Складывает два числа"""
    return a + b

def test_add():
    """Тест сложения"""
    assert add(2, 3) == 5
```

Результат: ~95-100/100 (A)

---

### Пример 3: Сравнение версий

**Файл 1 (старая версия):**
```python
def process(data):
    result = []
    for item in data:
        result.append(item * 2)
    return result
```

**Файл 2 (новая версия):**
```python
def process(data):
    """Обрабатывает данные"""
    return [item * 2 for item in data]

def test_process():
    """Тест обработки"""
    assert process([1, 2, 3]) == [2, 4, 6]
```

Сравнение покажет: Файл 2 победитель (выше балл за документацию и тесты)

---

## 🔧 Технические детали

### Используемые библиотеки

- **Streamlit** - фреймворк веб-приложения
- **pandas** - таблицы сравнения
- **src.metrics** - бэкенд анализа

### Конфигурация страницы

```python
st.set_page_config(
    page_title="Code Quality Analyzer",
    page_icon="🔍",
    layout="wide"
)
```

### Производительность

- Анализ кода: ~0.5-2 секунды
- Сравнение файлов: ~1-3 секунды
- Генерация отчёта: мгновенно

---

## ❓ FAQ

**Q: Можно ли анализировать несколько файлов одновременно?**  
A: Нет, но можно использовать CLI: `python src/main.py --directory folder/`

**Q: Сохраняется ли история анализов?**  
A: Нет, каждый анализ независим. Используйте экспорт отчётов для сохранения.

**Q: Какой максимальный размер файла?**  
A: Streamlit по умолчанию ограничивает 200 MB, но рекомендуется до 10,000 строк.

**Q: Работает ли офлайн?**  
A: Да, после установки зависимостей интернет не нужен.

---

## 🐛 Известные ограничения

- Анализируется только Python код
- Не поддерживается Python 2.x
- Некоторые сложные конструкции могут не распознаваться

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте, что Streamlit установлен: `pip list | grep streamlit`
2. Перезапустите приложение: `Ctrl+C` затем `streamlit run src/web_app.py`
3. Откройте issue на GitHub

---

**Приятного использования! 🚀**
